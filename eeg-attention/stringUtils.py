"""def replaceLastOccurrence(string, oldOcc, newOcc):
    i = len(string) - 1
    if oldOcc in string:
        while (i > 0 and str(string[i]) != oldOcc):
            i -= 1
        
        return string[0: i] + newOcc + string[i + 1:]
    return string"""
    